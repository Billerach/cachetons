<div id="new_job_profile_container">
  <h2>Nouveau modèle de fiche de paie</h2>

  <%= form_with(model: @job_profile, url: job_profiles_path, local: true) do |f| %>
    <div id="new_job_profile_infos">
      <%= f.label :name %>
      <%= f.text_field "job_profile[name]" %>
      <%= f.label "Artiste ? " %>
      <%= f.check_box "job_profile[artist]" %>
      <%= f.label "Cadre ? " %>
      <%= f.check_box "job_profile[executive]" %>
    </div>
    <div id="new_job_profile_contributions">
      <% @contributions.each do |contribution| %>
        <label>
          <%= check_box_tag "job_profile[contributions_list][]", contribution.id %>
          <%= contribution.name %>
        </label>
        <br>
      <% end %>
    </div>
    <%= f.submit %>
  <% end %>
</div>
